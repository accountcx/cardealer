{
  "name": "@cardealer/core",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@cardealer/types": "workspace:*"
  },
  "devDependencies": {
    "@cardealer/config-typescript": "workspace:*",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
